/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Palmimo Portal
 * OpenAPI spec version: 0.1.0
 */
import type { AppJobInfo } from './appJobInfo';
import type { AppSourceInfo } from './appSourceInfo';
import type { AppSummaryStatus } from './appSummaryStatus';

/**
 * One app's ledger entry plus a coarse status.
 *
 * ``status`` is one of ``stopped``/``broken`` (ledger-derived) or
 * ``installing``/``updating``/``deleting`` (this app is the target of the
 * one in-flight ``apps.lock`` job). The systemd-derived states
 * (``running``, ``failed(exit_code)``) land with start/stop (design doc 3.5).
 */
export interface AppSummary {
  autostart: boolean;
  broken_reason: string | null;
  credential_rejected: boolean;
  exit_code?: number | null;
  installed_at: number | null;
  last_job: AppJobInfo | null;
  latest_commit: string | null;
  name: string;
  reason?: string | null;
  source: AppSourceInfo;
  status: AppSummaryStatus;
  update_available: boolean;
  url?: string | null;
}
