# env unknown key: "scope" is not one of required, description, help_url
schema = 1
name = "bad-app"
description = "test"
command = ["run"]

[env.API_KEY]
description = "an api key"
scope = "readonly"
